{
  "name": "@nx-arch/templates",
  "version": "0.0.0",
  "description": "Template definitions for nx-arch",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "node -e \"require('./dist/index.js')\""
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  }
}
