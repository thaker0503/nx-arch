export default function MarketingPage() {
  return (
    <main className="min-h-screen bg-white text-slate-900">
      <section className="mx-auto max-w-5xl px-6 py-24">
        <p className="text-sm uppercase tracking-[0.2em] text-slate-500">powered by nx-arch</p>
        <h1 className="mt-6 text-5xl font-semibold">{{projectName}}</h1>
        <p className="mt-4 text-xl text-slate-600">
          Build SaaS-ready marketing with {{ui}} UI components and {{api}} APIs.
        </p>
      </section>
    </main>
  );
}
