{
  "name": "@nx-arch/core",
  "version": "0.0.0",
  "description": "Core utilities for the nx-arch CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm && tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit",
    "test": "node ./dist/index.js --help || true"
  },
  "dependencies": {
    "@nx-arch/templates": "workspace:*",
    "chalk": "^5.3.0",
    "fs-extra": "^11.2.0",
    "globby": "^14.0.2"
  }
}
