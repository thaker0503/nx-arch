{
  "name": "nx-arch",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for the nx-arch CLI and supporting packages",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsup": "^7.2.0",
    "typescript": "^5.4.0"
  }
}
